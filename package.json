{
  "name": "rmb-server",
  "version": "0.0.0",
  "description": "\"RESTful API for rate my business backoffice\"",
  "main": "src/main.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsup-node src/main.ts dist/main.js --minify",
    "test": "test",
    "start": "npm run build && node dist/main.js"
  },
  "author": "BBee98",
  "license": "ISC",
  "devDependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/swagger": "^9.5.1",
    "@types/nodemailer": "^6.4.17",
    "dotenv": "^17.2.1",
    "handlebars": "^4.7.8",
    "jest": "^30.0.5",
    "nodemailer": "^7.0.5",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "fastify": "^5.4.0"
  }
}
